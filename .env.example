# Every environment uses PostgreSQL. Use pooled runtime and direct migration
# connections supplied by your own development database provider.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Required for Bearer session-token signing. Use a random 32+ character value.
# SESSION_SECRET is preferred; AUTH_SECRET, NEXTAUTH_SECRET, or JWT_SECRET also work.
SESSION_SECRET="replace-with-a-random-32-plus-character-secret"

NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Deployment-only initialization switches. Keep false unless intentionally
# running the guarded idempotent initializer for the matching environment.
RUN_PREVIEW_SEED="false"
RUN_PRODUCTION_INIT="false"
DEMO_SEED_PASSWORD=""

# Operator-only production content commands. Set these in the current process,
# run the named command, then unset them. Do not enable them in every build.
ALLOW_PRODUCTION_CONTENT_INSPECT="false"
ALLOW_PRODUCTION_CONTENT_INIT="false"

# Optional fictional content accounts. Leave blank to reuse existing users.
# When supplied, passwords must meet the server password policy and the new
# accounts are forced to change them at first login.
INIT_INSTRUCTOR_PASSWORD=""
INIT_MANAGER_PASSWORD=""
INIT_LEARNER_PASSWORD=""

# Public self-registration is disabled by default in production.
ALLOW_PUBLIC_REGISTRATION="false"

# Required only while running `npm run db:seed:admin` for the first time.
INITIAL_ADMIN_EMAIL=""
INITIAL_ADMIN_PASSWORD=""
INITIAL_ADMIN_USERNAME=""
INITIAL_ADMIN_FIRST_NAME=""
INITIAL_ADMIN_LAST_NAME=""

# Optional OneID placeholders:
ONEID_CLIENT_ID=""
ONEID_CLIENT_SECRET=""
ONEID_AUTH_URL=""
ONEID_REDIRECT_URI=""
ONEID_SCOPE=""
